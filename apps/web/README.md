# @coliseum/web

Vite + React SPA for Amphitheatre.

```bash
pnpm install
pnpm --filter @coliseum/web dev
```

The API at `localhost:3001` and the `/api` proxy (including WebSocket) are configured in `vite.config.ts`.
